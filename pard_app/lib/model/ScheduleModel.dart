import 'package:flutter/material.dart';

class ScheduleModel extends StatelessWidget {
  const ScheduleModel({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}